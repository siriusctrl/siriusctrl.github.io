---
import BaseLayout from "../layouts/BaseLayout.astro";
import ProjectArtwork from "../components/ProjectArtwork.astro";
import { projects } from "../data/projects";
import { getCollection } from "astro:content";

const freeform = projects[0];
const terminalProjects = projects.slice(1);
const notes = (await getCollection("notes", ({ data }) => !data.draft))
  .sort((a, b) => b.data.publishedAt.valueOf() - a.data.publishedAt.valueOf())
  .slice(0, 3);
---

<BaseLayout>
  <main>
    <section class="home-intro" aria-labelledby="hero-title">
      <div class="intro-rail">
        <p>Independent software</p>
        <p>Built in public</p>
      </div>
      <h1 id="hero-title">
        Software for seeing
        <span>what systems are doing.</span>
      </h1>
      <div class="intro-bottom">
        <p class="intro-statement">
          I like turning complicated ideas into working software—things you can run, inspect, and play with.
        </p>
        <div class="intro-links" aria-label="Homepage links">
          <a href="/projects/">View projects <i class="ph ph-arrow-right" aria-hidden="true"></i></a>
          <a href="https://github.com/siriusctrl">
            GitHub <i class="ph ph-arrow-up-right" aria-hidden="true"></i>
          </a>
        </div>
      </div>
    </section>

    <section class="section selected-work" aria-labelledby="selected-title">
      <div class="work-heading">
        <p class="section-index">01 — 04</p>
        <h2 id="selected-title">Recent work</h2>
        <p>Working software, recorded in the environment where it actually runs.</p>
      </div>

      <article class="home-featured">
        <a class="home-featured-media" href={`/projects/${freeform.slug}/`}>
          <ProjectArtwork project={freeform} eager={true} />
        </a>
        <div class="home-featured-copy">
          <div class="project-meta"><span>01</span><span>{freeform.kind}</span><span>{freeform.year}</span></div>
          <h3><a href={`/projects/${freeform.slug}/`}>{freeform.name}</a></h3>
          <p>{freeform.detail}</p>
          <div class="project-links">
            <a href={`/projects/${freeform.slug}/`}>
              View project <i class="ph ph-arrow-right" aria-hidden="true"></i>
            </a>
            {freeform.demoUrl && (
              <a href={freeform.demoUrl}>
                Open demo <i class="ph ph-arrow-up-right" aria-hidden="true"></i>
              </a>
            )}
          </div>
        </div>
      </article>

      <div class="project-ledger">
        {terminalProjects.map((project, index) => (
          <article class="ledger-project">
            <span class="ledger-number">{String(index + 2).padStart(2, "0")}</span>
            <a class="ledger-media" href={`/projects/${project.slug}/`} tabindex="-1" aria-hidden="true">
              <ProjectArtwork project={project} />
            </a>
            <div class="ledger-copy">
              <div class="project-meta"><span>{project.kind}</span><span>{project.year}</span></div>
              <h3><a href={`/projects/${project.slug}/`}>{project.name}</a></h3>
              <p>{project.summary}</p>
            </div>
            <a class="ledger-arrow" href={`/projects/${project.slug}/`} aria-label={`Read about ${project.name}`}>
              <i class="ph ph-arrow-up-right" aria-hidden="true"></i>
            </a>
          </article>
        ))}
      </div>
    </section>

    <section class="section notes-preview" aria-labelledby="notes-title">
      <div class="section-heading compact-heading">
        <h2 id="notes-title">Notes</h2>
        <p>Architecture choices, experiments, and the reasoning that survives the commit.</p>
      </div>
      <div class="note-list">
        {notes.map((note) => (
          <a class="note-row" href={`/notes/${note.id}/`}>
            <time datetime={note.data.publishedAt.toISOString()}>
              {note.data.publishedAt.toLocaleDateString("en", { month: "short", day: "numeric", year: "numeric" })}
            </time>
            <span>
              <strong>{note.data.title}</strong>
              <small>{note.data.description}</small>
            </span>
            <i class="ph ph-arrow-right" aria-hidden="true"></i>
          </a>
        ))}
      </div>
      <a class="text-link" href="/notes/">All notes <i class="ph ph-arrow-right" aria-hidden="true"></i></a>
    </section>
  </main>
</BaseLayout>
