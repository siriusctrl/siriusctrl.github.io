{
  "name": "siriusctrl.github.io",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "serve:test": "node scripts/serve-test.mjs",
    "generate:theme-svg": "node scripts/generate-theme-svg-variants.mjs",
    "check:theme-svg": "node scripts/generate-theme-svg-variants.mjs --check",
    "check": "npm run check:theme-svg && astro check && astro build",
    "verify:ui": "playwright test",
    "verify:proof": "node scripts/record-proof.mjs",
    "setup:browsers": "playwright install chromium webkit"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.3",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/geist": "^5.3.0",
    "@fontsource-variable/geist-mono": "^5.3.0",
    "@fontsource-variable/noto-serif-sc": "^5.3.0",
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.61.1",
    "typescript": "^6.0.3"
  }
}
